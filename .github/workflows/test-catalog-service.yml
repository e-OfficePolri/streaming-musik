name: Test catalog-service

on:
  push:
    branches: [main]
    paths:
      - "apps/catalog-service/**"
      - ".github/workflows/test-catalog-service.yml"
  pull_request:
    paths:
      - "apps/catalog-service/**"
      - ".github/workflows/test-catalog-service.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/catalog-service

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Jalankan test
        run: npm test -- --coverage

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: catalog-service-coverage
          path: apps/catalog-service/coverage/
